# 01 — Scaffold & Config

A configured Nuxt 4 project with TypeScript strict mode and devtools enabled.

## What to explore

- `nuxt.config.ts` — central config file
- TypeScript strict mode catches more errors at compile time
- Nuxt DevTools panel (press Shift+Alt+D in dev mode)

## Run

```bash
npm install
npm run dev
```
